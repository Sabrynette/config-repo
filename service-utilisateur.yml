server:
  port: 8082
spring:
  application:
    name: service-utilisateur
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    fetch-registry: true
    register-with-eureka: true